.header 
  .title
    = link_to "cafe app", '/', class: "header__title--text"
    %i.fas.fa-coffee 
  .name-box
    .user-name 
      = current_user.name
    .box  
      .box_user_edit
        = link_to new_review_path(current_user) do
          = icon('fas','edit',class: 'icon')
      .box_cog
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog',class: 'icon')